import React from 'react'
import ArrowLink from '../ui/animations/ArrowLink'
import { STRINGS } from '.'

const MoreProjects = () => {
  return <ArrowLink href="https://github.com/marinakinalone?tab=repositories" label={STRINGS.cta} />
}

export default MoreProjects
